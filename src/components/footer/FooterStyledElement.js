import styled from "styled-components";

export const FooterContainer = styled.div`
  display: flex;
  align-items: center;
  justify-content: space-evenly;
  background-color: var(--clr-dark);
  color: var(--clr-light);
  grid-area: footer;
`;
export const FooterElements = styled.h2`
  color: var(--clr-primary-500);
  font-size: 1.3rem;
`;


